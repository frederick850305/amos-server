# amos-server

AMOS 后端服务（Spring Boot 3.2.5 + Flyway）。为 `amos` 前端（github.com/frederick850305/amos）提供 REST API，管理船舶 / 海工资产的维护、库存、采购与预算数据。

## 技术栈

- Java 17（本机 Temurin 17）+ Maven 多模块：`amos-app`（应用）、`amos-common`（公共模块）
- Spring Boot 3.2.5
- Flyway 统一管理数据库迁移，**H2 与 PostgreSQL 双兼容**
- 默认 profile 连本地容器化 PostgreSQL（`localhost:5432/amos`）

## 运行

### 1. 本地 PostgreSQL（默认）

前置：本机用 OrbStack 跑 PostgreSQL 17 开发库（容器 `pg17-local`，库名 `amos`，账号 `postgres/postgres`），参考 `docs/postgres-setup.md`。

```bash
./scripts/start.sh          # 默认 postgres profile，连 localhost:5432/amos
```

> `scripts/start.sh` 会自动设置 `JAVA_HOME`（读取仓库根 `.java-version` 指向本机 Temurin）并进入 `amos-app` 模块，无需手动 export。

启动后健康检查：`GET http://localhost:8080/actuator/health`

### 2. H2 快速验证 / 演示（双跑）

无需本地 PG，用内置 H2 内存库（`MODE=PostgreSQL`，与 PG 跑同一套 Flyway 迁移）：

```bash
./scripts/start.sh --spring.profiles.active=default --server.port=8081
```

日志出现 `Successfully applied N migrations ... now at version vN` 且 `/actuator/health` 返回 200，即说明迁移在 H2 上同样跑通。详细搭建与双跑说明见 `docs/postgres-setup.md`。

## 数据库迁移

迁移脚本位于 `amos-app/src/main/resources/db/migration/`（`V1__init.sql`、`V2__component_type_children.sql`），统一使用 `BIGINT GENERATED BY DEFAULT AS IDENTITY` 等 H2/PG 双兼容写法，保证两端同源。

## 文档

- `docs/postgres-setup.md` —— 本地 PostgreSQL 搭建、Flyway 双兼容约定与 H2 双跑验证
