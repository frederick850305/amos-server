spring:
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: localhost:8848
        file-extension: yaml
      discovery:
        enabled: false
        server-addr: localhost:8848
